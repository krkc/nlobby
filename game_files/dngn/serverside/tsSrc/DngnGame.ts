/// <reference path="node.d.ts"/>

import * as events from "events";

export class Game {
  //eventEmitter = new events.EventEmitter();  /* Event emitter object */
  constructor(p1: string, p2: string) {
    console.log('TEST!!');
  }
}
